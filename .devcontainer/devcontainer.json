{
    "name": "PHP",
    "image": "mcr.microsoft.com/devcontainers/php:8.2",
    "forwardPorts": [8000],
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y php8.2-cli curl && cp -r .devcontainer/workspace/. ~/ && curl -fLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim && vim +PlugInstall +qall",
    "customizations": {
        "vscode": {
            "extensions": ["vscodevim.vim"]
        }
    }
}